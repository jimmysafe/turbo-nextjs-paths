{
  "buildCommand": "pnpm --workspace-root exec turbo --no-daemon --filter org build --env-mode loose",
  "installCommand": "node -v && pnpm --workspace-root --filter org... install --config.dedupe-peer-dependents=false",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}
