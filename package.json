{
  "name": "turbo-nextjs-paths",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "clean": "rm -rf .next && rm -rf .turbo",
    "lint": "turbo lint",
    "ui": "pnpm --filter @repo/ui ui",
    "prepare": "husky",
    "lint-staged": "lint-staged --shell"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "catalog:dev-deps",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=18",
    "npm": "please-use-pnpm",
    "yarn": "please-use-pnpm",
    "pnpm": ">= 9.12.0"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": "turbo lint --"
  }
}
